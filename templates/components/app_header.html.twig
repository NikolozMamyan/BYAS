

<header
    class="app-header"
    data-controller="header-sync"
    data-header-sync-failed-message-value="{{ 'passport.sync_status.failed'|trans }}"
>
    <div class="app-header__bar">
        <div class="app-header__side app-header__side--left">
            <button type="button" class="app-header__icon" aria-label="{{ 'passport.sync'|trans }}" data-header-sync-target="button" data-action="header-sync#sync">
                <img src="{{ asset('img/nav-top/top_bar_refrech.png') }}" alt="">
            </button>

            <a
                href="{{ path('app_front_play_history') }}"
                class="app-header__icon"
                aria-label="{{ 'app_header.activity'|trans }}"
            >
                <img src="{{ asset('img/nav-top/top_bar_pulse.png') }}" alt="">
            </a>
        </div>

        <div class="app-header__center">
            <a href="{{ path('app_front_passport') }}" class="app-header__brand" aria-label="{{ 'app_header.home'|trans }}">
                <img src="{{ asset('img/logo/BYAS LOGO.png') }}" alt="BYAS">
            </a>
        </div>

        <div class="app-header__side app-header__side--right">
            <a href="{{ path('app_front_passport_settings') }}" class="app-header__avatar-link" aria-label="{{ 'app_header.settings'|trans }}" data-level="{{ app.user.globalLevel|default('') }}">
                {% include 'components/user_avatar.html.twig' with {
                    avatarUrl: app.user.avatarUrl,
                    displayName: app.user.displayName|default(app.user.userIdentifier),
                    avatarClass: 'app-header__avatar'
                } only %}
            </a>
        </div>
    </div>

</header>
